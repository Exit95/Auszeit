# Secrets Template - NICHT MIT ECHTEN WERTEN COMMITTEN!
# Erstelle die Secrets manuell auf dem Cluster:
#
# kubectl create secret generic s3-credentials -n websites \
#   --from-literal=S3_ENDPOINT=http://minio:9000 \
#   --from-literal=S3_BUCKET=danapfel-digital \
#   --from-literal=S3_ACCESS_KEY=CHANGE_ME \
#   --from-literal=S3_SECRET_KEY=CHANGE_ME \
#   --from-literal=S3_REGION=us-east-1 \
#   --from-literal=S3_ACCESS_KEY_ID=CHANGE_ME \
#   --from-literal=S3_SECRET_ACCESS_KEY=CHANGE_ME
#
# kubectl create secret generic danapfel-secrets -n websites \
#   --from-literal=SESSION_SECRET=CHANGE_ME \
#   --from-literal=TOTP_SECRET=CHANGE_ME \
#   --from-literal=ADMIN_PASSWORD_HASH=CHANGE_ME
#
# kubectl create secret generic galabau-secrets -n websites \
#   --from-literal=ADMIN_PASSWORD=CHANGE_ME
#
# kubectl create secret generic hartmann-secrets -n websites \
#   --from-literal=SMTP_HOST=CHANGE_ME \
#   --from-literal=SMTP_PORT=587 \
#   --from-literal=SMTP_USER=CHANGE_ME \
#   --from-literal=SMTP_PASS=CHANGE_ME \
#   --from-literal=SMTP_TO=CHANGE_ME
#
# kubectl create secret generic keramik-secrets -n websites \
#   --from-literal=ADMIN_PASSWORD=CHANGE_ME \
#   --from-literal=SUPERUSER_PASSWORD=CHANGE_ME \
#   --from-literal=TOTP_SECRET=CHANGE_ME \
#   --from-literal=SMTP_HOST=CHANGE_ME \
#   --from-literal=SMTP_PORT=587 \
#   --from-literal=SMTP_USER=CHANGE_ME \
#   --from-literal=SMTP_PASS=CHANGE_ME \
#   --from-literal=STRIPE_SECRET_KEY=CHANGE_ME \
#   --from-literal=STRIPE_PUBLISHABLE_KEY=CHANGE_ME \
#   --from-literal=DB_PASSWORD=CHANGE_ME
#
# kubectl create secret generic mariadb-secrets -n websites \
#   --from-literal=MARIADB_ROOT_PASSWORD=CHANGE_ME \
#   --from-literal=MARIADB_PASSWORD=CHANGE_ME \
#   --from-literal=MARIADB_USER=auszeit_user \
#   --from-literal=MARIADB_DATABASE=auszeit_prod
